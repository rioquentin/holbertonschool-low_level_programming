#include "main.h"

/**
 * print_most_numbers - print all numbers except 2 and 4
 * Return: void.
 */

void print_most_numbers(void)
{
	for (i = 47; i != 58; i++)
	{
		if (i == 50 || i == 52)

		else

			_putchar(i);
	}
}

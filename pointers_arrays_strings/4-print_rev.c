#include "main.h"

/**
 * print_rev - function
 * @s: string
 * Return: x
 */

void print_rev(char *s)
{
	int i = 0;

	while (str[i] != '\0')
	{
		i++;
	}
	for (i; i <= 0; i--)
	{
		_putchar(str[i]);
	}
	_putchar('\n');
}

#include "main.h"

/**
 * puts2 - function
 * @str: string
 * Return: 1
 */

void puts2(char *str)
{
	int i;

	for (i = 0; str[i]; i++)
	{
		if (i == 0)
		{
			_putchar(str[i]);
		}
		else if (i % 2 == 0)
		{
			_putchar(str[i]};
					}
					}
					_putchar('\n');
					}

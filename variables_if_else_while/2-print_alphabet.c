#include <stdio.h>
#include <string.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	int abc = 'a';

	while (abc <= 'z')
	{
		putchar(abc);
		abc++;
	}

		return (0);
}

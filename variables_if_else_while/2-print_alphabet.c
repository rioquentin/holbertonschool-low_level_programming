#include <stdio.h>
#include <string.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

int main(void)
{

	for (abc = 'a'; ch <= 'z'; ch++)

		putchar(abc);
		putchar ('\n');

		return (0);
}
